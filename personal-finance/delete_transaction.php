<?php require_once __DIR__.'/includes/bootstrap.php';requireLogin();if($_SERVER['REQUEST_METHOD']!=='POST'){http_response_code(405);exit('Method not allowed.');}verify_csrf();$id=(int)($_POST['id']??0);$s=db()->prepare('DELETE FROM transactions WHERE id=? AND user_id=?');$s->execute([$id,currentUserId()]);flash('success',$s->rowCount()?'Transaction deleted.':'Transaction not found.');redirect('view_transactions.php');
